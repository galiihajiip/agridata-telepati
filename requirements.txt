# Minimal dependencies for the current project stage (Block 1: bootstrap).
# Heavier stack (torch, opencv, object-detection framework, etc.) is added
# in later blocks once the dataset audit determines actual requirements —
# per the master spec, the stack must not be locked in before that audit.
numpy==2.1.3
PyYAML==6.0.2
pytest==8.3.4
