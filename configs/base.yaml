# Konfigurasi dasar pipeline TELEPATI 8.0 AgriData.
# Seluruh path bersifat relatif terhadap akar project agar pipeline portabel
# antar mesin, tanpa path personal maupun path absolut.

seed: 42

project:
  name: "agridata-telepati8"

dataset:
  root: "Telepati 8.0 Datasets"
  train_dir: "train"
  valid_dir: "valid"
  test_dir: "test"
  annotation_filename: "_annotations.coco.json"
